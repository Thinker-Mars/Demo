<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cone.sunny.dao.ExcelDao">
	
			
		<!-- 新增图书信息 -->
		    <insert id="bookMetaExcel" >
		     insert into book_work_add(name,author_tag,price,publish_company,star_level,brief,publish_time,productid,is_audit,apply_time,apply_worker_id,model)
		     
		      values(#{name},#{author},#{price},#{publishCompany},
		      #{star},#{brief},#{time},#{productid},#{is_audit},#{apply_time},#{workerid},#{model})
		    </insert>
</mapper>